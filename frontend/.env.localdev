# npm run dev:local 전용. 로컬에서 직접 띄운 백엔드(기본 포트 8080)를 가리킨다.
# JDK17로 백엔드 실행: JAVA_HOME=<jdk17 경로> ./gradlew bootRun
# (로컬 Postgres 또는 SPRING_DATASOURCE_URL로 Supabase를 직접 가리켜도 됨)
VITE_API_BASE_URL=http://localhost:8080
